# Codex Relay 产品定义

## 产品定位

Codex Relay 是一个本地 Windows 运维工具，不是聊天产品或模型市场。它让 Codex 用户保留真实官方账号，同时把用户自己的第三方 API 路线加入同一个模型选择入口。

一句话价值：**官方额度可用时走官方，用户需要时手动热切到第三方，不再为普通切换反复退出登录、改配置或重启 Router。**

## 目标用户

- 同时使用 Codex 官方订阅和第三方 API 的个人开发者；
- 已使用 CC Switch 或手动中转，但希望减少身份切换和重启的人；
- 需要确认请求实际上游、状态、耗时和 Token 的用户；
- 重视现有聊天、插件、配置与恢复能力的 Windows 用户。

## 核心产品契约

1. 模型容量固定为两个官方容量位加最多五个第三方容量位。
2. 官方槽位只基于真实 Codex 官方认证；第三方槽位只基于用户保存的供应商 Key。
3. 一个供应商可以复用到多个槽位，每个槽位由用户独立命名。
4. 只有配置完整且有 Key 的第三方槽位才发布到 Codex；空槽不成为无响应菜单项。
5. 普通模型切换发生在 Codex 模型栏，Relay 不自动根据额度切换付费路线。
6. 保存模型不接管 Codex；只有显式应用才建立快照并修改 `config.toml`。
7. 显式恢复回到本次进入 Relay 前的 Codex config/auth，但保留 Relay 的模型、供应商和 Key。
8. Relay 不迁移、合并、删除或回滚聊天和插件数据。
9. 外部工具完整接管时只报告，不自动抢回。
10. UI 只显示可验证事实，不伪造额度、登录、模型身份或 Token。

## 关键场景

| 场景 | 产品行为 |
| --- | --- |
| 已登录官方账号 | 发布两个官方候选槽位和已配置第三方；官方请求使用官方账号。 |
| 未登录 | 不发布官方槽位；允许只使用一至五个第三方槽位。 |
| Relay 中再登录 | 检测真实认证并更新模型目录；Desktop 刷新方式以实机结果为准。 |
| 官方额度不足 | 用户手动选择第三方，登录身份不被第三方 Key 替换。 |
| 多个第三方互切 | 同一路线尽量续接，跨路线重放便携上下文。 |
| CC Switch 接管 | 显示外部接管，不自动修改配置；由用户决定重新应用或恢复。 |
| 关闭窗口 | 隐藏到托盘，Router 继续运行。 |
| 退出程序 | 不自动恢复；Relay 仍应用时先警告 Router 将停止。 |
| 恢复 | 恢复进入 Relay 前的 config/auth；保留 Relay 设置、聊天和插件。 |

## 上下文承诺

- 官方到官方优先使用官方响应链，若官方拒绝才进行一次便携回退。
- 同一第三方 Responses 路线可使用该供应商的响应链。
- Chat Completions 使用 Relay 维护的可见 messages。
- 跨路线只重放可见对话、工具结果和任务信息，不共享隐藏推理或 foreign response ID。
- 重放可能增加输入 Token；目标窗口不足时会裁剪较旧轮次。

因此，产品承诺的是“尽量让任务可继续并把续接方式说清楚”，不是“所有模型无损共享同一上下文”。

## 成功标准

- 用户能一眼确认当前是 Relay、外部接管还是未应用。
- 真实官方账号页、额度、插件和两个官方模型在 Relay 模式下可用。
- 至少一个真实 Responses 与一个真实 Chat Completions 供应商通过流式、工具和取消验收。
- 普通模型切换无需重写配置或重启 Router。
- 凭据不串线，日志不泄露秘密或正文。
- 恢复后 config/auth 可验证，Relay 设置与聊天/插件保留。
- 软件能从安装版和便携版稳定运行，关闭与退出语义清楚。

## 非目标

- 云端团队控制台；
- 自动额度判断和静默切换；
- 绕过认证、地区或服务条款；
- 第三方伪装官方；
- 合并 Codex 内部历史数据库；
- 保证所有 OpenAI-compatible 服务完整兼容；
- 保证跨模型零 Token、零延迟或无损上下文。

## 品牌与文案

品牌性格是克制、清楚、可信。Codex Relay 是一个需要承担配置风险的工作工具，文案应先说状态和结果，再给动作。

禁止使用未经证实的表达，例如“官方无损”“百分百恢复”“自动识别所有模型”“Token 完全一致”。正确文案应指出上游、认证来源、验证状态和下一步。

## 当前发布状态

产品仍处于开发与实机验收阶段。Router、流式转换、快照恢复、四页 UI 和 Electron 桌面壳已有代码；完整测试回归、真实官方体验、第三方兼容矩阵、桌面打包与视觉验收尚未全部完成。

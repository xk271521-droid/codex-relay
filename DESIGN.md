# Codex Relay 界面设计规范

## 设计方向

Codex Relay 应像一款安静、可靠的 Windows 配置工具：状态明确、信息紧凑、操作可预测。它不是模型商城，也不是营销网站，不使用夸张英雄区、装饰性渐变、卡片堆叠或密集供应商 Logo。

用户打开软件后首先需要知道三件事：

1. Codex 现在是否真的由 Relay 控制；
2. 官方登录和可用模型是什么状态；
3. 下一步是应用、配置、修复还是恢复。

## 应用框架

- 默认窗口：1180×780。
- 最小窗口：920×640。
- 左侧固定窄导航，右侧为页面标题、全局动作和内容区。
- 顶部全局动作仅保留“保存并应用到 Codex”和“恢复使用前状态”。
- 窄屏时内容改为单列并允许页面滚动，固定格式的槽位、按钮和表格需要稳定尺寸，不能因动态文案跳动。

## 信息架构

### 概览

只回答当前运行状态：

- Relay 未应用 / 已应用 / 外部接管；
- Router 是否监听；
- Codex 当前提供商和实际 base URL；
- 官方账号已登录 / API Key 模式 / 未登录；
- 本次交接快照已创建 / 不存在；
- 两个官方容量位与五个第三方容量位；
- 最近请求概览。

概览页不能通过读取状态自动修改配置。若检测到外部接管，主文案应说明“Codex 当前可能由 CC Switch、官方直连或手动配置控制，Relay 不会自动覆盖”。

### 模型与供应商

采用两组连续列表，不做卡片中的卡片：

- 官方模型：两个只读容量位；未登录时明确显示“未登录”，不能显示“可用”。
- 第三方模型：固定五个管理槽位；空槽显示添加入口，完整槽显示名称、上游、供应商和状态。
- 供应商：独立列表，可被多个槽位复用。

普通模型对话框只展示：显示名称、供应商、上游模型。上下文窗口、图片、丢弃参数等放入高级区域。供应商普通配置只展示名称、API 地址、接口类型和 Key；完整 endpoint、自定义认证头、前缀及附加请求头放入高级区域。

清空槽位不删除供应商或 Key。删除供应商需要先解除所有槽位引用，并明确说明将删除对应本地加密 Key。

### 请求记录

使用密集但可扫读的表格/列表，列为：

- 时间；
- Codex 显示名 / 实际上游模型；
- 官方或第三方通道；
- HTTP 状态；
- 耗时；
- Token；
- 上下文模式。

Token 只显示上游实际返回的 usage。没有数据时显示“未返回”，不得显示 `0` 来暗示真实消耗为零。请求记录不展示 API Key、token 或消息正文。

上下文模式使用准确标签：官方原生续接、官方便携回退、第三方原生续接、第三方持续对话、便携上下文、新任务或未知。标签旁应有简短说明，而不是让用户猜测。

### 安全与恢复

包含：

- 会话文件有限保护状态；
- 可选的加密上下文持久化；
- 开机启动；
- 数据目录；
- 恢复使用前状态。

恢复区必须明确：恢复 config/auth；保留 Relay 供应商、槽位、名称、Key、上下文设置、聊天和插件。若配置已漂移或被外部工具接管，显示警告并要求二次确认。

## 全局状态模型

| 状态 | 主标题 | 主动作 |
| --- | --- | --- |
| 未配置路线 | 尚无可用模型 | 前往模型与供应商 |
| 已配置未应用 | Relay 已就绪，尚未应用到 Codex | 保存并应用到 Codex |
| 已应用 | Relay 模式正在服务 Codex | 更新 Relay 配置 |
| Relay 标记仍在但配置漂移 | Relay 配置需要修复 | 显式修复 |
| 外部完整接管 | Codex 已由其他配置接管 | 重新应用或保持现状 |
| Router 停止 | Codex 当前无法连接 Relay | 重新打开/启动 Router |
| 无官方登录 | 官方账号未登录 | 只用第三方或在 Codex 登录 |
| 官方认证失效 | 官方登录已失效 | 重新登录并同步 |

状态不能只靠颜色。每个状态都要有文字、图标和一个明确动作。

## 应用流程

“保存并应用到 Codex”打开预检对话框，列出：

- 可用路线和缺失项；
- 将写入 `model_provider`、`model`、`model_catalog_json` 和 `openai_base_url`；
- 是否创建本次使用前快照，或保留已有快照；
- 配置写入后可能需要刷新或重新打开 Codex；
- 检查失败时不会修改 Codex。

保存模型或供应商不得暗中触发应用。打开页面也不得自动修复或接管。

## 恢复流程

“恢复使用前状态”只有检测到完整交接快照时可用。确认对话框应写明：

- 将恢复本次进入 Relay 前的 `config.toml` 和 `auth.json`；
- Relay 期间的新登录可能被启用前认证替换；
- 不删除 Relay 配置、聊天或插件；
- 若外部工具已修改 Codex，恢复会覆盖当前配置并回到交接快照；
- 恢复后需要刷新或重新打开 Codex。

最终按钮默认禁用，用户勾选确认后才能执行。取消和右上角关闭始终可用，不要求填写任何字段。

## 桌面生命周期

- 关闭窗口：隐藏到系统托盘，Router 继续运行；首次关闭可给一次系统通知。
- 再次启动：单实例，将已有窗口显示并聚焦。
- 退出程序：不恢复 Codex；若 Relay 仍生效，先警告 Router 停止会让 Codex 暂时不可用。
- 开机启动：使用开关控制，后台启动并保留托盘入口。
- 端口被 Codex Relay 实例占用：复用现有服务；被其他应用占用：明确报错，不伪装成功。

## 视觉系统

```css
:root {
  --bg: #f6f6f3;
  --surface: #ffffff;
  --surface-subtle: #f1f1ed;
  --sidebar: #222420;
  --text: #242520;
  --muted: #686b63;
  --border: #d9dbd4;
  --primary: #b64f27;
  --success: #287a4b;
  --warning: #9a6518;
  --danger: #b33a32;
}
```

- 页面背景与内容表面保持中性，主色只用于主要命令和当前选择。
- 成功、警告、危险只表达语义，不把整个界面做成单一橙色或暗蓝色。
- 卡片圆角不超过 8px；页面区块优先使用无框分组和分隔线。
- 正文字号和紧凑标题固定，不随视口宽度缩放；字距为 0。
- 图标使用本地 Lucide 资源。熟悉动作优先用图标，陌生图标提供 tooltip 或可访问名称。

## 表单与文案

- 字段标签描述用户拥有的数据，例如“显示名称”“API 地址”“上游模型 ID”。
- 错误说明原因和下一步，例如“缺少 DeepSeek 的 API Key，Codex 配置未修改”。
- 不使用“连接正常”概括所有状态；分别显示 Router、配置指向、官方登录和上游请求结果。
- 不在 UI 中宣称“官方额度正常”，除非真实官方界面或接口提供可验证数据。
- 长 URL、模型名和错误消息允许换行或省略，不能挤压按钮和状态列。

## 可访问性与响应式验收

- 目标 WCAG 2.2 AA 对比度。
- 所有导航、表单、对话框、开关和确认操作可用键盘完成。
- 对话框具备标题、焦点管理、Escape 关闭和恢复焦点。
- 尊重 `prefers-reduced-motion`，不使用影响操作的装饰动画。
- 在 1180×780、920×640 和窄屏三组尺寸截图检查无重叠、截断、空白画布或横向失控。
- 最长供应商名、模型名、URL、Token 数和本地化错误文案均需压力测试。

## 当前验证状态

四页 UI 和 Electron 桌面壳已有代码，图标和 Lucide 已进入工程。发布前仍需完成完整回归、真实 Electron 截图、键盘与对话框检查、托盘/单实例/开机启动测试，以及安装版和便携版验证。任何视觉完成度结论都应以这些检查结果为准。
